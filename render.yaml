services:
  - type: web
    name: alexa-admin-02
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: RENDER_EXTERNAL_URL
        value: https://alexa-admin-02.onrender.com
